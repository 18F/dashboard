sudo: false

language: ruby
rvm:
  - 2.2.3


before_script:
  - bundle exec jekyll serve --detach

script: "rspec"

after_script: "pkill -f jekyll"

git:
  submodules: false

notifications:
  email:
    recipients:
      - gregory.boone@gsa.gov
    on_success: change
    on_failure: change

cache: bundler
