sudo: false

language: ruby
rvm:
  - 2.2.3


before_script:
  - bundle exec jekyll clean
  - bundle exec jekyll serve --detach
  - sleep 1
  - rspec

script: "pkill -f jekyll"
git:
  submodules: false

notifications:
  email:
    recipients:
      - gregory.boone@gsa.gov
    on_success: change
    on_failure: change

cache: bundler
